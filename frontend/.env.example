# Frontend env. Only NEXT_PUBLIC_* vars are exposed to the browser — never put secrets here.

# Base URL of the CirclePay API (Nest backend). Local dev backend runs on :4001.
NEXT_PUBLIC_API_URL=http://localhost:4001/api
